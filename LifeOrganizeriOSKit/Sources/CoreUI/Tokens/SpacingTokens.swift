import SwiftUI

// MARK: - Spacing Tokens

public extension CGFloat {
    /// Extra extra small spacing (2pt)
    /// Use for tight layouts and minimal separation
    static let lifeSpacingXXS: CGFloat = 2

    /// Extra small spacing (4pt)
    /// Use for minimal gaps between related elements
    static let lifeSpacingXS: CGFloat = 4

    /// Small spacing (8pt)
    /// Use for compact layouts and closely related items
    static let lifeSpacingSM: CGFloat = 8

    /// Medium spacing (16pt)
    /// Use for standard spacing between elements
    static let lifeSpacingMD: CGFloat = 16

    /// Large spacing (24pt)
    /// Use for generous spacing between sections
    static let lifeSpacingLG: CGFloat = 24

    /// Extra large spacing (32pt)
    /// Use for section separation and major layout divisions
    static let lifeSpacingXL: CGFloat = 32

    /// Extra extra large spacing (48pt)
    /// Use for major sections and primary layout divisions
    static let lifeSpacingXXL: CGFloat = 48
}
