# Place your domain entities here

Example:
```swift
public struct YourEntity: Codable, Equatable, Sendable, Identifiable {
    public let id: UUID
    public let name: String

    public init(id: UUID = UUID(), name: String) {
        self.id = id
        self.name = name
    }
}
```
